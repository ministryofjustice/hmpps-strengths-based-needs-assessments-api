{
  "name": "SAN API - Sign Assessment",
  "request": {
    "method": "POST",
    "urlPathPattern": "/san-api/assessment/[a-f0-9-]+/sign"
  },
  "response": {
    "status": 200,
    "headers": {
      "Content-Type": "application/json"
    },
    "jsonBody": {
      "metaData": {
        "uuid": "{{request.pathSegments.[2]}}",
        "createdAt": "{{now}}",
        "versionUuid": "{{randomValue type='UUID'}}",
        "versionNumber": 1,
        "versionCreatedAt": "{{now}}",
        "versionUpdatedAt": "{{now}}",
        "versionTag": "SELF_SIGNED",
        "formVersion": "1.0"
      },
      "assessment": {},
      "oasysEquivalent": {}
    },
    "transformers": ["response-template"]
  },
  "priority": 50
}
